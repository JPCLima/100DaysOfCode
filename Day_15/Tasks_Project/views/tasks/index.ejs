<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="grid-container">
        <div class="left-container">
            <h1 class="welcome">Welcome TODO App</h1>
            <p>
                <%= time %>
            </p>
            <div class="counter">
                Task to be done: <%= Object.keys(tasks).length %>
            </div>

        </div>
        <div class="right-container">
            <h1>Tasks<a href="/tasks/new" class="plus">+</a></h1>
            <ul>
                <% for( let t of tasks ) { %>
                    <li>
                        <a href="/tasks/<%= t.id %>">
                            <%= t.task %>
                        </a>
                    </li>

                    <% } %>
            </ul>

        </div>
    </div>

</body>

</html>